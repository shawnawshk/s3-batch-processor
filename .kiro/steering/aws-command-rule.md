when running aws cli command, always avoid pager. please use `--no-cli-pager`  at the end of the command to get the output directly.

always remember to stop all existing ecs tasks before start to run step function execution.

Do not use echo anymore before execution single aws command

Using region `ap-east-1`
